{
  "name": "@mindforge/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "seed:minimal": "tsx --conditions=development scripts/seed-minimal.ts",
    "seed:rich": "tsx --conditions=development scripts/seed-rich.ts",
    "test:integration": "vitest run",
    "postinstall": "prisma generate",
    "seed:report": "tsx --conditions=development scripts/seed-report.ts"
  },
  "dependencies": {
    "@mindforge/core": "workspace:*",
    "@prisma/adapter-pg": "^7.9.1",
    "@prisma/client": "^7.9.1"
  },
  "devDependencies": {
    "prisma": "^7.9.1",
    "tsx": "^4.23.7",
    "vitest": "^4.1.10"
  }
}
