{
  "name": "@mindforge/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "seed:minimal": "tsx scripts/seed-minimal.ts",
    "seed:rich": "tsx scripts/seed-rich.ts",
    "test:integration": "vitest run"
  },
  "dependencies": {
    "@prisma/client": "^7.9.1"
  },
  "devDependencies": {
    "prisma": "^7.9.1",
    "tsx": "^4.23.7",
    "vitest": "^4.1.10"
  }
}
